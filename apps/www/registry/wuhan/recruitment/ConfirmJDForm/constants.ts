/**
 * ConfirmJDForm 常量定义
 *
 * 集中管理组件的默认配置和固定文本
 */

/** 默认表单标题 */
export const DEFAULT_TITLE = "补充信息";

/** 默认提交按钮文本 */
export const DEFAULT_SUBMIT_BUTTON_TEXT = "确认";

/** 默认日期格式 */
export const DEFAULT_DATE_FORMAT = "YYYY-MM-DD";

/** InputNumber 默认配置 */
export const DEFAULT_INPUT_NUMBER_CONFIG = {
  min: 0,
  step: 1,
  precision: 2,
} as const;
